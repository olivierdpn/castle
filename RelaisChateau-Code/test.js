const rp = require('request-promise');
const cheerio = require('cheerio');
const XRegxp = require('xregexp');
var where = 0 ;
var data = require('./StarredRestaurant');

var text = data;

console.log(data);